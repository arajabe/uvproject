server {
    listen 80;
    server_name studentperformancerecordai.com www.studentperformancerecordai.com;

    location / {
        proxy_pass http://frontend:8501;   # Your frontend container
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
